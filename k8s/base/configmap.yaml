apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: libshary
data:
  BOOKSEARCH_GRPC: 'booksearch:50051'
  DATABASE_USER: admin
  REDIS_URL: 'redis://default:$(REDIS_PASSWORD)@redis:6379'
