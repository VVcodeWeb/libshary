name: build

on:
  workflow_run:
    workflows: ['test']
    types:
      - completed

permissions:
  actions: read
  contents: read

jobs:
  build:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [api, web, booksearch]
    name: Build ${{ matrix.service }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'
      - name: Install protoc
        run: sudo apt install protobuf-compiler

      - name: Install dependencies
        run: npm ci --cache --prefer-offline && npm install @bufbuild/protobuf

      - name: Run build
        run: npx nx run ${{ matrix.service }}:build:production

      - name: Build Docker image
        run: docker build -f apps/${{ matrix.service }}/Dockerfile -t libshary-${{ matrix.service }}:$GITHUB_SHA .
